export class Fighter {
  constructor(id, first_name, last_name, weightClass, pictureLink) {
    this.id = id;
    this.firstName = first_name;
    this.lastName = last_name;
    this.weightClass = weightClass;
    this.pictureLink = pictureLink;
  }
}
